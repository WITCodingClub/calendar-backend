development:
  adapter: async

test:
  adapter: test

# Nothing broadcasts or subscribes yet, and production is one Puma process, so
# the in-process adapter is enough. Use solid_cable when a second process needs
# to receive broadcasts.
production:
  adapter: async
