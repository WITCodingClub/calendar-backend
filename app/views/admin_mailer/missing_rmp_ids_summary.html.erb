<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      body {
        font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
        line-height: 1.6;
        color: #333;
        max-width: 600px;
        margin: 0 auto;
        padding: 20px;
      }
      .header {
        background: #d13732;
        color: white;
        padding: 20px;
        border-radius: 8px 8px 0 0;
      }
      .content {
        background: #f9f9f9;
        padding: 20px;
        border: 1px solid #ddd;
        border-top: none;
        border-radius: 0 0 8px 8px;
      }
      .faculty-list {
        background: white;
        border: 1px solid #ddd;
        border-radius: 4px;
        margin: 20px 0;
      }
      .faculty-item {
        padding: 12px 16px;
        border-bottom: 1px solid #eee;
      }
      .faculty-item:last-child {
        border-bottom: none;
      }
      .faculty-name {
        font-weight: 600;
        color: #d13732;
      }
      .faculty-email {
        color: #666;
        font-size: 14px;
      }
      .courses-count {
        color: #888;
        font-size: 14px;
      }
      .button {
        display: inline-block;
        background: #d13732;
        color: white;
        padding: 12px 24px;
        text-decoration: none;
        border-radius: 4px;
        margin: 20px 0;
      }
      .summary {
        background: #fff3cd;
        border: 1px solid #ffc107;
        padding: 16px;
        border-radius: 4px;
        margin: 20px 0;
      }
    </style>
  </head>
  <body>
    <div class="header">
      <h1 style="margin: 0;">Weekly RMP Summary</h1>
      <p style="margin: 5px 0 0 0;">Calendar Backend Admin</p>
    </div>

    <div class="content">
      <div class="summary">
        <strong><%= @count %></strong> faculty member<%= 's' if @count != 1 %> currently missing Rate My Professor IDs
      </div>

      <p>The following faculty members do not have RMP IDs assigned:</p>

      <div class="faculty-list">
        <% @faculties.each do |faculty| %>
          <div class="faculty-item">
            <div class="faculty-name"><%= faculty.full_name %></div>
            <div class="faculty-email"><%= faculty.email %></div>
            <div class="courses-count"><%= faculty.courses.count %> course<%= 's' if faculty.courses.count != 1 %></div>
          </div>
        <% end %>
      </div>

      <p>
        <a href="<%= missing_rmp_ids_admin_faculties_url %>" class="button">
          Fill Missing RMP IDs
        </a>
      </p>

      <hr style="border: none; border-top: 1px solid #ddd; margin: 30px 0;">

      <p style="font-size: 14px; color: #666;">
        <strong>Next Steps:</strong>
      </p>
      <ol style="font-size: 14px; color: #666;">
        <li>Click "Auto Search" to automatically find matching professors on RMP</li>
        <li>Click "Manual Search" to review potential matches and assign manually</li>
        <li>If not found, submit the professor to RMP using the form links provided</li>
      </ol>

      <p style="font-size: 12px; color: #888; margin-top: 30px;">
        This is an automated weekly summary sent every Sunday.
        The nightly sync job will automatically pick up newly approved professors from RMP.
      </p>
    </div>
  </body>
</html>
