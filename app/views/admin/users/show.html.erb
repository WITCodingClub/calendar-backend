<div class="flex justify-between items-center mb-6">
  <h1 class="text-2xl font-bold">User Details</h1>
  <%= link_to "Back to Users", admin_users_path, class: "glass-button px-4 py-2 rounded text-sm cursor-pointer inline-block" %>
</div>

  <div class="glass-card shadow-lg rounded-lg overflow-hidden">
    <div class="px-6 py-5">
      <dl class="grid grid-cols-1 gap-x-4 gap-y-6 sm:grid-cols-2">
        <div>
          <dt class="text-sm font-medium text-gray-500">First Name</dt>
          <dd class="mt-1 text-sm text-gray-900"><%= @user.first_name %></dd>
        </div>

        <div>
          <dt class="text-sm font-medium text-gray-500">Last Name</dt>
          <dd class="mt-1 text-sm text-gray-900"><%= @user.last_name %></dd>
        </div>

        <div>
          <dt class="text-sm font-medium text-gray-500">Email</dt>
          <dd class="mt-1 text-sm text-gray-900"><%= @user.email %></dd>
        </div>

        <div>
          <dt class="text-sm font-medium text-gray-500">Access Level</dt>
          <dd class="mt-1">
            <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-[#d13732]/10 text-[#d13732]">
              <%= @user.access_level_text %>
            </span>
          </dd>
        </div>

        <% if policy(@user).toggle_support_flag? %>
        <div class="sm:col-span-2">
          <dt class="text-sm font-medium text-gray-500 mb-2">Support Tools</dt>
          <dd class="mt-1 flex flex-wrap gap-2">
            <% env_switcher_enabled = Flipper.enabled?(FlipperFlags::ENV_SWITCHER, @user) %>
            <% debug_mode_enabled = Flipper.enabled?(FlipperFlags::DEBUG_MODE, @user) %>

            <%= button_to toggle_support_flag_admin_user_path(@user, flag: :env_switcher),
                method: :post,
                class: "inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full text-xs font-semibold transition-colors #{env_switcher_enabled ? 'bg-green-100 text-green-800 hover:bg-green-200' : 'bg-gray-100 text-gray-600 hover:bg-gray-200'}",
                title: env_switcher_enabled ? "Click to disable" : "Click to enable" do %>
              <span class="w-2 h-2 rounded-full <%= env_switcher_enabled ? 'bg-green-500' : 'bg-gray-400' %>"></span>
              Env Switcher
            <% end %>

            <%= button_to toggle_support_flag_admin_user_path(@user, flag: :debug_mode),
                method: :post,
                class: "inline-flex items-center gap-1.5 px-3 py-1.5 rounded-full text-xs font-semibold transition-colors #{debug_mode_enabled ? 'bg-green-100 text-green-800 hover:bg-green-200' : 'bg-gray-100 text-gray-600 hover:bg-gray-200'}",
                title: debug_mode_enabled ? "Click to disable" : "Click to enable" do %>
              <span class="w-2 h-2 rounded-full <%= debug_mode_enabled ? 'bg-green-500' : 'bg-gray-400' %>"></span>
              Debug Mode
            <% end %>
          </dd>
        </div>
        <% end %>

        <div>
          <dt class="text-sm font-medium text-gray-500">Created At</dt>
          <dd class="mt-1 text-sm text-gray-900"><%= @user.created_at.strftime("%B %d, %Y at %I:%M %p") %></dd>
        </div>

        <div>
          <dt class="text-sm font-medium text-gray-500">Updated At</dt>
          <dd class="mt-1 text-sm text-gray-900"><%= @user.updated_at.strftime("%B %d, %Y at %I:%M %p") %></dd>
        </div>
      </dl>

      <div class="mt-6 flex gap-3 flex-wrap">
        <% if policy(@user).edit? %>
          <%= link_to "Edit User", edit_admin_user_path(@user), class: "glass-button px-4 py-2 rounded text-sm cursor-pointer inline-block" %>
        <% end %>

        <% if policy(@user).force_calendar_sync? && @user.google_credential&.google_calendar %>
          <%= button_to "Force Calendar Sync", force_calendar_sync_admin_user_path(@user),
            method: :post,
            data: { turbo_confirm: "Force sync calendar for #{@user.email}? This will update all events regardless of changes." },
            class: "glass-button px-4 py-2 rounded text-sm cursor-pointer bg-blue-50/40 text-blue-600" %>
        <% end %>

        <% if policy(@user).destroy? %>
          <%= render "delete_confirmation_modal",
              user: @user,
              button_text: "Delete User",
              button_class: "glass-button px-4 py-2 rounded text-sm cursor-pointer bg-red-600 text-white" %>
        <% end %>
      </div>
    </div>
  </div>

  <!-- Enrollments Section -->
  <div class="mt-8 glass-card shadow-lg rounded-lg overflow-hidden">
    <div class="px-6 py-5">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-xl font-bold">Enrollments</h2>
        <span class="text-sm text-gray-600">Total: <%= @user.enrollments.count %></span>
      </div>

      <% if @user.enrollments.any? %>
        <div class="space-y-4" data-controller="semester-switcher">
          <% if @enrollments_by_term.size > 1 %>
            <div class="flex items-center gap-2 mb-4">
              <label for="enrollment-semester-select" class="text-sm font-medium text-gray-700">Select Semester:</label>
              <select
                id="enrollment-semester-select"
                data-semester-switcher-target="dropdown"
                data-action="change->semester-switcher#switch"
                class="px-4 py-2 border border-gray-300 rounded-md shadow-sm focus:outline-none focus:ring-2 focus:ring-opacity-50"
                style="background: rgba(255, 255, 255, 0.8); backdrop-filter: blur(3px); color: #d13732; border-color: rgba(209, 55, 50, 0.3);">
                <% @enrollments_by_term.each_with_index do |(term, enrollments), index| %>
                  <option value="<%= index %>"><%= term.name %> (<%= enrollments.count %> course<%= enrollments.count == 1 ? '' : 's' %>)</option>
                <% end %>
              </select>
            </div>
          <% end %>

          <% @enrollments_by_term.each do |term, enrollments| %>
            <div class="mb-6" data-semester-switcher-target="semester">
              <h3 class="text-lg font-semibold mb-3 text-[#d13732]"><%= term.name %></h3>
              <div class="overflow-x-auto">
                <table class="min-w-full divide-y divide-gray-200">
                  <thead class="bg-white/50">
                    <tr>
                      <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">CRN</th>
                      <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Course</th>
                      <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Title</th>
                      <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Type</th>
                      <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Credits</th>
                    </tr>
                  </thead>
                  <tbody class="divide-y divide-gray-200 bg-white/30">
                    <% enrollments.each do |enrollment| %>
                      <tr>
                        <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-900">
                          <%= enrollment.course.crn %>
                        </td>
                        <td class="px-4 py-2 whitespace-nowrap text-sm font-medium text-gray-900">
                          <%= enrollment.course.subject %> <%= enrollment.course.course_number %>
                        </td>
                        <td class="px-4 py-2 text-sm text-gray-900">
                          <%= enrollment.course.title %>
                        </td>
                        <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-700">
                          <%= enrollment.course.schedule_type %>
                        </td>
                        <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-700">
                          <%= enrollment.course.credit_hours %>
                        </td>
                      </tr>
                    <% end %>
                  </tbody>
                </table>
              </div>
            </div>
          <% end %>
        </div>
      <% else %>
        <p class="text-gray-600">No enrollments found for this user.</p>
      <% end %>
    </div>
  </div>

  <!-- OAuth Credentials Section -->
  <div class="mt-8 glass-card shadow-lg rounded-lg overflow-hidden">
    <div class="px-6 py-5">
      <div class="flex justify-between items-center mb-4">
        <h2 class="text-xl font-bold">OAuth Credentials</h2>
        <span class="text-sm text-gray-600">Total: <%= @oauth_credentials.size %></span>
      </div>

      <% if @oauth_credentials.any? %>
        <div class="overflow-x-auto">
          <table class="min-w-full divide-y divide-gray-200">
            <thead class="bg-white/50">
              <tr>
                <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Email</th>
                <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Provider</th>
                <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Status</th>
                <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Calendar</th>
                <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Created</th>
                <th scope="col" class="px-4 py-2 text-left text-xs font-medium text-gray-700 uppercase tracking-wider">Actions</th>
              </tr>
            </thead>
            <tbody class="divide-y divide-gray-200 bg-white/30">
              <% @oauth_credentials.each do |credential| %>
                <tr>
                  <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-900">
                    <%= credential.email %>
                  </td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-700">
                    <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-blue-100 text-blue-800">
                      <%= credential.provider.capitalize %>
                    </span>
                  </td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm">
                    <% if credential.token_expired? %>
                      <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-red-100 text-red-800">
                        Expired
                      </span>
                    <% else %>
                      <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-green-100 text-green-800">
                        Active
                      </span>
                    <% end %>
                  </td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-700">
                    <% if credential.google_calendar %>
                      <span class="px-2 inline-flex text-xs leading-5 font-semibold rounded-full bg-[#d13732]/10 text-[#d13732]">
                        <%= credential.events_count %> events
                      </span>
                    <% else %>
                      <span class="text-gray-400">None</span>
                    <% end %>
                  </td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm text-gray-700">
                    <%= credential.created_at.strftime("%b %d, %Y") %>
                  </td>
                  <td class="px-4 py-2 whitespace-nowrap text-sm">
                    <div class="flex gap-2">
                      <% if policy(@user).refresh_oauth_credential? %>
                        <%= button_to "Refresh", refresh_oauth_credential_admin_user_path(@user, credential_id: credential.id),
                          method: :post,
                          class: "text-blue-600 hover:text-blue-900 font-medium" %>
                      <% end %>
                      <% if policy(@user).revoke_oauth_credential? %>
                        <%= button_to "Revoke", revoke_oauth_credential_admin_user_path(@user, credential_id: credential.id),
                          method: :delete,
                          data: { turbo_confirm: "⚠️ Revoke OAuth access for #{credential.email}? This will delete the associated calendar (#{credential.events_count} events) and cannot be undone." },
                          class: "text-red-600 hover:text-red-900 font-medium" %>
                      <% end %>
                      <% unless policy(@user).refresh_oauth_credential? || policy(@user).revoke_oauth_credential? %>
                        <span class="text-gray-400 text-sm">—</span>
                      <% end %>
                    </div>
                  </td>
                </tr>
              <% end %>
            </tbody>
          </table>
        </div>
      <% else %>
        <p class="text-gray-600">No OAuth credentials found for this user.</p>
      <% end %>
    </div>
  </div>
