<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <title><%= content_for(:title) || "Admin" %> - WIT Calendar</title>

    <%= yield :head %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>


    <style>
      body {
          background-image: url('<%= asset_path("ia-no-logo.png") %>');
        background-size: cover;
        background-position: center;
        background-attachment: fixed;
        margin: 0;
        padding: 0;
        font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
        min-height: 100vh;
      }

      .gradient-overlay {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        z-index: -1;
      }

      .admin-container {
        display: flex;
        justify-content: center;
        align-items: flex-start;
        min-height: 80vh;
        padding: 10px;
        position: relative;
      }

      .admin-card {
        background: rgba(255, 255, 255, 0.5);
        backdrop-filter: blur(8px);
        -webkit-backdrop-filter: blur(8px);
        border-radius: 20px;
        border: 1px solid rgba(209, 55, 50, 0.4);
        box-shadow: 0 8px 32px rgba(209, 55, 50, 0.15);
        width: 100%;
        max-width: 1200px;
        margin-top: 20px;
        overflow: hidden;
      }

      .admin-header {
        background: linear-gradient(135deg, rgba(209, 55, 50, 0.15) 0%, rgba(255, 255, 255, 0.5) 100%);
        backdrop-filter: blur(5px);
        -webkit-backdrop-filter: blur(5px);
        color: #18181A;
        padding: 20px 30px;
        border-bottom: 1px solid rgba(209, 55, 50, 0.4);
        text-shadow: 0 1px 2px rgba(255, 255, 255, 0.5);
      }

      .admin-nav {
        background: rgba(255, 255, 255, 0.4);
        backdrop-filter: blur(5px);
        -webkit-backdrop-filter: blur(5px);
        border-bottom: 2px solid rgba(209, 55, 50, 0.3);
        padding: 0;
      }

      .admin-nav-list {
        display: flex;
        flex-wrap: wrap;
        margin: 0;
        padding: 0;
        list-style: none;
      }

      .admin-nav-item {
        margin: 0;
        border-right: 1px solid rgba(209, 55, 50, 0.4);
      }

      .admin-nav-item:first-child {
        border-left: 2px solid transparent;
        border-color: rgba(209, 55, 50, 0.4);
      }

      .admin-nav-item:last-child {
        border-right: 2px solid transparent;
        border-color: rgba(209, 55, 50, 0.4);
      }

      .admin-nav-link {
        display: block;
        padding: 12px 24px;
        color: #2C3440;
        text-decoration: none;
        border-bottom: 3px solid transparent;
        transition: all 0.3s ease;
        font-weight: 500;
        text-shadow: 0 1px 2px rgba(255, 255, 255, 0.2);
        position: relative;
      }

      .admin-nav-link::after {
        content: '';
        position: absolute;
        right: 0;
        top: 10%;
        height: 80%;
        width: 1px;
        background: rgba(209, 55, 50, 0.3);
        transition: all 0.3s ease;
      }

      .admin-nav-item:last-child .admin-nav-link::after {
        display: none;
      }

      .admin-nav-link:hover,
      .admin-nav-link.active {
        color: #2C3440;
        background: linear-gradient(135deg, rgba(255, 255, 255, 0.8) 0%, rgba(209, 55, 50, 0.1) 100%);
        border-bottom-color: #d13732;
        box-shadow: 0 2px 8px rgba(209, 55, 50, 0.2);
      }

      .admin-nav-link:hover::after,
      .admin-nav-link.active::after {
        background: rgba(209, 55, 50, 0.5);
        box-shadow: 0 0 4px rgba(209, 55, 50, 0.3);
      }

      .admin-content {
        padding: 30px;
        background: rgba(255, 255, 255, 0.3);
        backdrop-filter: blur(3px);
        -webkit-backdrop-filter: blur(3px);
        color: #1f2937;
        text-shadow: 0 1px 2px rgba(255, 255, 255, 0.5);
      }

      .admin-content h1,
      .admin-content h2,
      .admin-content h3,
      .admin-content h4,
      .admin-content h5,
      .admin-content h6 {
        color: #111827;
        text-shadow: 0 1px 2px rgba(255, 255, 255, 0.7);
      }

      .glass-button {
        background: rgba(255, 255, 255, 0.4) !important;
        backdrop-filter: blur(5px) !important;
        -webkit-backdrop-filter: blur(5px) !important;
        border: 2px solid #d13732 !important;
        color: #2C3440 !important;
        text-shadow: 0 1px 2px rgba(255, 255, 255, 0.7) !important;
        transition: all 0.3s ease !important;
        position: relative !important;
        font-weight: 600 !important;
      }

      .glass-button:hover {
        background: linear-gradient(135deg, #d13732 0%, #b02d29 100%) !important;
        border-color: #f26961 !important;
        transform: translateY(-1px) !important;
        box-shadow: 0 6px 16px rgba(209, 55, 50, 0.3) !important;
        color: #FFFFFF !important;
        text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2) !important;
      }

      .admin-user-info {
        display: flex;
        align-items: center;
        justify-content: space-between;
        flex-wrap: wrap;
        gap: 15px;
      }

      .admin-user-details {
        flex: 1;
      }

      .admin-user-name {
        font-size: 18px;
        font-weight: 600;
        margin: 0;
      }

      .admin-user-role {
        font-size: 14px;
        opacity: 0.9;
        margin: 0;
      }

      @media (max-width: 768px) {
        .admin-container {
          padding: 10px;
        }

        .admin-card {
          margin-top: 10px;
        }

        .admin-header {
          padding: 15px 20px;
        }

        .admin-content {
          padding: 20px;
        }
      }
    </style>
  </head>

  <body>
    <div class="gradient-overlay"></div>

    <div class="admin-container">
      <div class="admin-card">
        <!-- Header -->
        <div class="admin-header">
          <div class="admin-user-info">
            <div class="admin-user-details">
              <h1 class="admin-user-name">WIT Calendar Admin</h1>
              <p class="admin-user-role">Logged in as <%= current_user.full_name %> (<%= current_user.access_level_text %>)</p>
            </div>
            <div style="display: flex; gap: 10px;">
              <%= button_to "Sign Out", destroy_user_session_path,
                  method: :delete,
                  class: "glass-button px-4 py-2 rounded text-sm cursor-pointer" %>
            </div>
          </div>
        </div>

        <!-- Navigation -->
        <nav class="admin-nav">
          <ul class="admin-nav-list">
            <li class="admin-nav-item">
              <%= link_to "Dashboard", admin_root_path,
                  class: "admin-nav-link #{request.path == admin_root_path ? 'active' : ''}" %>
            </li>
            <li class="admin-nav-item">
              <%= link_to "Users", admin_users_path,
                  class: "admin-nav-link #{request.path.start_with?('/admin/users') ? 'active' : ''}" %>
            </li>
            <% if current_user.super_admin? || current_user.owner? %>
              <li class="admin-nav-item">
                <%= link_to "Flipper", "/admin/flipper",
                    class: "admin-nav-link #{request.path.start_with?('/admin/flipper') ? 'active' : ''}" %>
              </li>
            <% end %>
            <li class="admin-nav-item">
              <%= link_to "Blazer", "/admin/blazer",
                  class: "admin-nav-link #{request.path.start_with?('/admin/blazer') ? 'active' : ''}" %>
            </li>
            <li class="admin-nav-item">
              <%= link_to "Performance", "/admin/performance",
                  class: "admin-nav-link #{request.path.start_with?('/admin/performance') ? 'active' : ''}" %>
            </li>
            <li class="admin-nav-item">
              <%= link_to "Jobs", "/admin/jobs",
                  class: "admin-nav-link #{request.path.start_with?('/admin/jobs') ? 'active' : ''}" %>
            </li>
          </ul>
        </nav>

        <!-- Main Content -->
        <div class="admin-content">
          <%= yield %>
        </div>
      </div>
    </div>

    <!-- Flash Messages -->
    <% flash.each do |type, message| %>
      <div class="fixed top-4 right-4 z-50 max-w-sm">
        <div class="bg-white border border-gray-200 rounded-lg shadow-lg p-4">
          <div class="flex items-center">
            <div class="flex-1">
              <p class="text-sm text-gray-900"><%= message %></p>
            </div>
            <button onclick="this.parentElement.parentElement.parentElement.remove()"
                    class="ml-4 text-gray-400 hover:text-gray-600">
              <span class="sr-only">Close</span>
              ×
            </button>
          </div>
        </div>
      </div>
    <% end %>

    <script>
      document.addEventListener('DOMContentLoaded', () => {
        const flashMessages = document.querySelectorAll('.fixed.top-4.right-4');
        flashMessages.forEach(message => {
          setTimeout(() => {
            if (message.parentElement) {
              message.remove();
            }
          }, 5000);
        });
      });
    </script>
  </body>
</html>
