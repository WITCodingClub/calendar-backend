<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <title><%= content_for(:title) || "Admin" %> - WIT Calendar</title>

    <%= yield :head %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="m-0 p-0 min-h-screen bg-cover bg-center bg-fixed" style="background-image: url('<%= asset_path("ia-no-logo.png") %>'); font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;">
    <div class="gradient-overlay"></div>

    <!-- Sidebar Layout -->
    <%= render "shared/admin_sidebar" %>

    <!-- Main content with sidebar offset (w-64 + left-4 + right margin) -->
    <main class="lg:ml-72 lg:mr-4 min-h-screen">
      <!-- Header -->
      <div class="bg-white/80 backdrop-blur-md border border-white/20 rounded-2xl px-6 py-4 sticky top-4 z-20 mt-4 shadow-lg" style="box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);">
        <div class="flex items-center justify-between">
          <div>
            <h1 class="text-xl font-semibold text-gray-900 m-0">WIT Calendar Admin</h1>
            <p class="text-sm text-gray-600 m-0">Logged in as <%= current_user.full_name %> (<%= current_user.access_level_text %>)</p>
          </div>
          <div class="flex gap-3 items-center">
            <div class="hidden md:flex items-center gap-1 text-xs text-gray-600">
              <span>Quick nav:</span>
              <kbd class="px-2 py-1 bg-gray-100 border border-gray-300 rounded text-xs">⌘K</kbd>
            </div>
            <%= button_to "Sign Out", destroy_user_session_path,
                method: :delete,
                class: "px-4 py-2 bg-red-600 text-white rounded hover:bg-red-700 text-sm cursor-pointer transition" %>
          </div>
        </div>
      </div>

      <!-- Content Area -->
      <div class="mt-4 mb-4 bg-white/80 backdrop-blur-md border border-white/20 rounded-2xl px-6 py-6 shadow-lg min-h-[calc(100vh-12rem)]" style="box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);">
        <%= yield %>
      </div>
    </main>

    <!-- Flash Messages -->
    <%= render "shared/flash_messages" %>

    <!-- Command Palette -->
    <%= render "shared/command_palette" %>
  </body>
</html>
