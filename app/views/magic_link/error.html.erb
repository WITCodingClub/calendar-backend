<!DOCTYPE html>
<html>
<head>
  <title>Authentication Error - WIT Calendar</title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 20px;
    }

    .container {
      background: white;
      border-radius: 16px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
      max-width: 500px;
      width: 100%;
      padding: 40px;
      text-align: center;
    }

    .error-circle {
      width: 80px;
      height: 80px;
      margin: 0 auto 20px;
      border-radius: 50%;
      background: #ef4444;
      display: flex;
      align-items: center;
      justify-content: center;
      animation: scaleIn 0.3s ease-out;
    }

    .error-icon {
      font-size: 48px;
      color: white;
    }

    @keyframes scaleIn {
      from {
        transform: scale(0);
      }
      to {
        transform: scale(1);
      }
    }

    h1 {
      color: #1f2937;
      margin-bottom: 12px;
      font-size: 28px;
      font-weight: 700;
    }

    .error-message {
      color: #ef4444;
      line-height: 1.6;
      margin-bottom: 16px;
      font-weight: 500;
      background: #fee2e2;
      padding: 16px;
      border-radius: 8px;
      border-left: 4px solid #ef4444;
    }

    p {
      color: #6b7280;
      line-height: 1.6;
      margin-bottom: 16px;
    }

    .close-button {
      margin-top: 24px;
      padding: 12px 24px;
      background: #667eea;
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      font-weight: 600;
      cursor: pointer;
      transition: background 0.2s;
    }

    .close-button:hover {
      background: #5568d3;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="error-circle">
      <div class="error-icon">✕</div>
    </div>

    <h1>Authentication Error</h1>

    <div class="error-message">
      <%= @error %>
    </div>

    <p>Please return to your extension and request a new magic link.</p>

    <button class="close-button" onclick="window.close()">Close this tab</button>
  </div>
</body>
</html>
