<% content_for :title, "Authentication Error - WIT Calendar" %>

<div class="min-h-screen flex items-center justify-center p-5">
  <div class="bg-white rounded-2xl shadow-2xl max-w-lg w-full p-10 text-center">
    <div class="w-20 h-20 mx-auto mb-5 rounded-full bg-red-500 flex items-center justify-center animate-[scaleIn_0.3s_ease-out]">
      <div class="text-5xl text-white">✕</div>
    </div>

    <h1 class="text-gray-900 mb-3 text-3xl font-bold">Authentication Error</h1>

    <div class="text-red-500 leading-relaxed mb-4 font-medium bg-red-50 p-4 rounded-lg border-l-4 border-red-500">
      <%= @error %>
    </div>

    <p class="text-gray-600 leading-relaxed mb-4">Please request a new magic link to sign in.</p>

    <%= link_to "Back to Sign In", new_user_session_path, class: "mt-6 inline-block px-6 py-3 bg-indigo-500 text-white border-0 rounded-lg text-base font-semibold cursor-pointer transition-all hover:bg-indigo-600" %>
  </div>
</div>

<style>
  @keyframes scaleIn {
    from { transform: scale(0); }
    to { transform: scale(1); }
  }
</style>
